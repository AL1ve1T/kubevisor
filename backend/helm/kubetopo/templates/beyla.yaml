{{- if .Values.beyla.enabled }}
{{- if .Values.beyla.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubetopo.beyla.serviceAccountName" . }}
  namespace: {{ include "kubetopo.namespace" . }}
  labels:
    app: {{ include "kubetopo.beyla.fullname" . }}
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: beyla
    {{- include "kubetopo.labels" . | nindent 4 }}
---
{{- end }}
{{- if .Values.beyla.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubetopo.beyla.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: beyla
    {{- include "kubetopo.labels" . | nindent 4 }}
rules:
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["list", "watch"]
  - apiGroups: [""]
    resources: ["pods", "services", "nodes"]
    verbs: ["list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubetopo.beyla.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: beyla
    {{- include "kubetopo.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubetopo.beyla.serviceAccountName" . }}
    namespace: {{ include "kubetopo.namespace" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubetopo.beyla.fullname" . }}
---
{{- end }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubetopo.beyla.fullname" . }}
  namespace: {{ include "kubetopo.namespace" . }}
  labels:
    app: {{ include "kubetopo.beyla.fullname" . }}
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: beyla
    {{- include "kubetopo.labels" . | nindent 4 }}
data:
  beyla-config.yml: |
    otel_traces_export:
      endpoint: {{ include "kubetopo.beyla.otlpEndpoint" . }}
    otel_metrics_export:
      endpoint: {{ include "kubetopo.beyla.otlpEndpoint" . }}
      features:
        - network
    network:
      enable: true
      source: {{ .Values.beyla.network.source }}
    ebpf:
      context_propagation: {{ .Values.beyla.contextPropagation }}
    javaagent:
      enabled: false
    attributes:
      kubernetes:
        enable: true
      select:
        beyla_network_flow_bytes:
          include:
            - k8s.src.owner.name
            - k8s.src.namespace
            - k8s.dst.owner.name
            - k8s.dst.namespace
            - dst.port
    {{- if .Values.beyla.discovery }}
    discovery:
      instrument:
        {{- range .Values.beyla.discovery }}
        - k8s_namespace: {{ .namespace }}
          open_ports: {{ .openPorts | quote }}
        {{- end }}
    {{- end }}
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "kubetopo.beyla.fullname" . }}
  namespace: {{ include "kubetopo.namespace" . }}
  labels:
    app: {{ include "kubetopo.beyla.fullname" . }}
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: beyla
    {{- include "kubetopo.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ include "kubetopo.beyla.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "kubetopo.beyla.fullname" . }}
        app.kubernetes.io/name: {{ include "kubetopo.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: beyla
    spec:
      hostPID: true
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      serviceAccountName: {{ include "kubetopo.beyla.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.beyla.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.beyla.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.beyla.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: beyla
          image: "{{ .Values.beyla.image.repository }}:{{ .Values.beyla.image.tag }}"
          imagePullPolicy: {{ .Values.beyla.image.pullPolicy }}
          securityContext:
            privileged: true
          env:
            - name: BEYLA_CONFIG_PATH
              value: "/config/beyla-config.yml"
          volumeMounts:
            - name: beyla-config
              mountPath: /config
          resources:
            {{- toYaml .Values.beyla.resources | nindent 12 }}
      volumes:
        - name: beyla-config
          configMap:
            name: {{ include "kubetopo.beyla.fullname" . }}
{{- end }}
