{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubetopo.serviceAccountName" . }}
  namespace: {{ include "kubetopo.namespace" . }}
  labels:
    app: {{ include "kubetopo.backend.fullname" . }}
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: backend
    {{- include "kubetopo.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- if .Values.rbac.create }}
---
# The backend lists pods cluster-wide to read pod status, restart counts, and
# pod IPs (PodStatusScraper / KubernetesPodWatcher).
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubetopo.backend.fullname" . }}-pod-reader
  labels:
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: backend
    {{- include "kubetopo.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubetopo.backend.fullname" . }}-pod-reader
  labels:
    app.kubernetes.io/name: {{ include "kubetopo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: backend
    {{- include "kubetopo.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubetopo.serviceAccountName" . }}
    namespace: {{ include "kubetopo.namespace" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubetopo.backend.fullname" . }}-pod-reader
{{- end }}
