apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubevisor-backend
  namespace: kubevisor
  labels:
    app: kubevisor-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubevisor-backend
  template:
    metadata:
      labels:
        app: kubevisor-backend
    spec:
      serviceAccountName: kubevisor-backend
      containers:
        - name: kubevisor-backend
          image: kubevisor-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 4318
              name: otlp-http
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: KUBEVISOR_OTLP_HTTP_PORT
              value: "4318"
            - name: KUBEVISOR_STALE_THRESHOLD_SECONDS
              value: "120"
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            - name: DB_HOST
              value: "kubevisor-postgres"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: kubevisor-postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kubevisor-postgres-secret
                  key: POSTGRES_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
